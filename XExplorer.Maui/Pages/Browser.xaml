<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:XExplorer.Core.ViewModel.Browser;assembly=XExplorer.Core"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:sync="clr-namespace:Syncfusion.Maui.Themes;assembly=Syncfusion.Maui.Core"
             xmlns:syfButton="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             xmlns:syfCore="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:inputs="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             xmlns:syfCarousel="clr-namespace:Syncfusion.Maui.Carousel;assembly=Syncfusion.Maui.Carousel"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:inputLayout="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:converts="clr-namespace:XExplorer.Core.Converts;assembly=XExplorer.Core"
             SizeChanged="MainGrid_OnSizeChanged"
             x:Class="XExplorer.Maui.Pages.Browser">
    <ContentPage.Resources>
        <sync:SyncfusionThemeResourceDictionary x:Key="SyncfusionTheme" />
        <converts:MinusConverter x:Key="WidthMinusConverter" />
        <converts:WebNavigatedEventArgsToUrlConverter x:Key="WebArgsConverter" />
    </ContentPage.Resources>
    <ContentPage.BindingContext>
        <vm:BrowserViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <VerticalStackLayout x:Name="MainView">
            <syfCore:SfBusyIndicator IsRunning="{Binding Processing}"
                                     Margin="10,20" HeightRequest="{Binding MainViewHeight}"
                                     AnimationType="DoubleCircle">
                <VerticalStackLayout>
                    <sfPopup:SfPopup AppearanceMode="{Binding AppearanceMode}"
                                     ShowFooter="True"
                                     OverlayMode="Blur"
                                     AnimationMode="Fade"
                                     StaysOpen="True"
                                     ShowCloseButton="True"
                                     HeaderTitle="提醒"
                                     DeclineCommand="{Binding DeclineCommand}"
                                     AcceptCommand="{Binding AcceptCommand}"
                                     IsOpen="{Binding IsShow}">
                        <sfPopup:SfPopup.PopupStyle>
                            <sfPopup:PopupStyle FooterBackground="LightGray"
                                                MessageTextColor="#292929"
                                                AcceptButtonBackground="DimGray"
                                                AcceptButtonTextColor="White"
                                                DeclineButtonBackground="DimGray"
                                                DeclineButtonTextColor="White"
                                                FooterButtonCornerRadius="0,20,20,0" />
                        </sfPopup:SfPopup.PopupStyle>
                        <sfPopup:SfPopup.ContentTemplate>
                            <DataTemplate>
                                <Label Text="{Binding Message}" TextColor="#292929"
                                       HorizontalTextAlignment="Center" Padding="10" />
                            </DataTemplate>
                        </sfPopup:SfPopup.ContentTemplate>
                    </sfPopup:SfPopup>
                    <sfPopup:SfPopup x:Name="popupImg" AppearanceMode="OneButton"
                      ShowFooter="True"
                      OverlayMode="Blur"
                      StaysOpen="False"
                      AnimationMode="Fade"
                      ShowCloseButton="True"
                      IsFullScreen="True"
                      HeaderTitle="下载记录"
                      IsOpen="{Binding ShowImg}">
                        <sfPopup:SfPopup.PopupStyle>
                            <sfPopup:PopupStyle FooterBackground="LightGray"
                                 MessageTextColor="#292929"
                                 AcceptButtonBackground="DimGray"
                                 AcceptButtonTextColor="White"
                                 DeclineButtonBackground="DimGray"
                                 DeclineButtonTextColor="White"
                                 FooterButtonCornerRadius="0,20,20,0" />
                        </sfPopup:SfPopup.PopupStyle>
                        <sfPopup:SfPopup.ContentTemplate>
                            <DataTemplate>
                                <Image Source="{Binding SelectedImg}"
                        WidthRequest="{Binding Source={x:Reference MainView}, Path=WidthRequest}"
                        HeightRequest="{Binding Source={x:Reference MainView}, Path=HeightRequest}" />
                            </DataTemplate>
                        </sfPopup:SfPopup.ContentTemplate>
                    </sfPopup:SfPopup>
                    <HorizontalStackLayout VerticalOptions="Center" HeightRequest="40" Padding="0,0,0,0">
                        <inputs:SfAutocomplete Text="{Binding Address, Mode=TwoWay}" 
                                             HeightRequest="35"
                                             WidthRequest="500"
                                             Margin="10,0,0,0"
                                             VerticalTextAlignment="Center"
                                             HorizontalTextAlignment="Start"
                                             ItemsSource="{Binding HistoryUrls}" />
                        <inputs:SfComboBox SelectedItem="{Binding SelectedDir}"
                           ItemsSource="{Binding Dirs}"
                           SelectedIndex="0"
                           DisplayMemberPath="ValidName"
                           VerticalTextAlignment="Center"
                           HorizontalTextAlignment="Start"
                           Margin="10,0"
                           MinimumWidthRequest="300" />
                        <syfButton:SfButton Text="回退" CornerRadius="10"
                                FontSize="18"
                                HeightRequest="40"
                                Padding="0,0,0,0"
                                Margin="10,0,0,0"
                                Command="{Binding GoBackCommand}">
                            <syfButton:SfButton.Background>
                                <LinearGradientBrush>
                                    <LinearGradientBrush.GradientStops>
                                        <GradientStop Color="#0D62D4" Offset="0" />
                                        <GradientStop Color="#9F3CDC" Offset="1" />
                                    </LinearGradientBrush.GradientStops>
                                </LinearGradientBrush>
                            </syfButton:SfButton.Background>
                        </syfButton:SfButton>
                        <syfButton:SfButton Text="前进" CornerRadius="10"
                                 FontSize="18"
                                 HeightRequest="40"
                                 Padding="0,0,0,0"
                                 Margin="10,0,0,0"
                                 Command="{Binding GoForwardCommand}">             
                            <syfButton:SfButton.Background>
                                <LinearGradientBrush>
                                    <LinearGradientBrush.GradientStops>
                                        <GradientStop Color="#0D62D4" Offset="0" />
                                        <GradientStop Color="#9F3CDC" Offset="1" />
                                    </LinearGradientBrush.GradientStops>
                                </LinearGradientBrush>
                            </syfButton:SfButton.Background>
                        </syfButton:SfButton>

                        <syfButton:SfButton Text="跳转" CornerRadius="10"
                                            FontSize="18"
                                            HeightRequest="40"
                                            Padding="0,0,0,0"
                                            Margin="10,0,0,0"
                                            Command="{Binding GotoCommand}">
                            <syfButton:SfButton.Background>
                                <LinearGradientBrush>
                                    <LinearGradientBrush.GradientStops>
                                        <GradientStop Color="#0D62D4" Offset="0" />
                                        <GradientStop Color="#9F3CDC" Offset="1" />
                                    </LinearGradientBrush.GradientStops>
                                </LinearGradientBrush>
                            </syfButton:SfButton.Background>
                        </syfButton:SfButton>
                                               
                        <syfButton:SfButton Text="下载" CornerRadius="10"
                                        FontSize="18"
                                        HeightRequest="40"
                                        Padding="0,0,0,0"
                                        Margin="10,0,0,0"
                                        Command="{Binding DownloadVideoCommand}">
                            <syfButton:SfButton.Background>
                                <LinearGradientBrush>
                                    <LinearGradientBrush.GradientStops>
                                        <GradientStop Color="#0D62D4" Offset="0" />
                                        <GradientStop Color="#9F3CDC" Offset="1" />
                                    </LinearGradientBrush.GradientStops>
                                </LinearGradientBrush>
                            </syfButton:SfButton.Background>
                        </syfButton:SfButton>
                        
                    </HorizontalStackLayout>
                    <BoxView StyleClass="Divider" />
                    <Grid VerticalOptions="Center">
                        <WebView Source="{Binding WebUrl, Mode=TwoWay}" 
                         HeightRequest="{Binding MainViewHeight}" 
                                 HorizontalOptions="Fill"
                                 VerticalOptions="Fill"
                                 BackgroundColor="Yellow"
                         WidthRequest="{Binding Source={x:Reference MainView}, Path=WidthRequest}">
                            <WebView.Behaviors>
                                <toolkit:EventToCommandBehavior
                                    EventName="Navigated"
                                    Command="{Binding UpdateUrlCommand}"  
                                    EventArgsConverter="{StaticResource WebArgsConverter}"/>
                            </WebView.Behaviors>
                        </WebView>
                    </Grid>
                    <HorizontalStackLayout MaximumHeightRequest="20">
                        <Label Text="{Binding Message}" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </syfCore:SfBusyIndicator>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>